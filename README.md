# mock-backend

Used to test integration with frontend

## Prerequisites

Download the relevant csv and save it as data.csv in the same directory as `app.py`

## Usage

`python3 app.py`

Requests to `http://localhost:5000/api?drug=[drug]&operation=[operation]`
